<Window x:Class="CMCS_Prototype.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CMCS_Prototype"
        mc:Ignorable="d"
        Title="CMCS - Claim Management System" Height="750" Width="1200"
        FontFamily="Segoe UI" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <DataTemplate DataType="{x:Type local:LoginView}">
            <local:LoginView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:LecturerView}">
            <local:LecturerView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:AdminView}">
            <local:AdminView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#005A8D" Padding="15">
            <TextBlock Text="Claim Management &amp; Certification System (CMCS)" 
                       FontSize="28" FontWeight="Bold" Foreground="White"/>
        </Border>

        <ContentControl Grid.Row="1" Content="{Binding CurrentView}"/>
    </Grid>
</Window>