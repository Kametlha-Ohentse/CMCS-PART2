<UserControl x:Class="CMCS_Prototype.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="1180">
    <StackPanel Margin="10">
        <TextBlock Text="Administrator Dashboard: Pending Claims for Review" FontSize="18" FontWeight="Bold" Foreground="#DC3545" Margin="0,0,0,10"/>

        <DataGrid ItemsSource="{Binding PendingClaims}" AutoGenerateColumns="False" IsReadOnly="True" Height="550">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Claim ID" Binding="{Binding ClaimId}" Width="Auto"/>
                <DataGridTextColumn Header="Lecturer" Binding="{Binding LecturerName}" Width="1.5*"/>
                <DataGridTextColumn Header="Month" Binding="{Binding MonthYear}" Width="Auto"/>
                <DataGridTextColumn Header="Hours" Binding="{Binding HoursWorked}" Width="Auto"/>
                <DataGridTextColumn Header="Rate (R)" Binding="{Binding HourlyRate, StringFormat=N2}" Width="Auto"/>
                <DataGridTextColumn Header="Amount (R)" Binding="{Binding TotalAmount, StringFormat=N2}" FontWeight="Bold" Width="Auto"/>
                <DataGridTextColumn Header="Notes" Binding="{Binding AdditionalNotes}" Width="2*"/>

                <DataGridTextColumn Header="Docs" Binding="{Binding Documents.Count, StringFormat='({0})'}" Width="Auto"/>

                <DataGridTemplateColumn Header="Action" Width="2*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Approve" Margin="5" Padding="5,2" Background="#28A745" Foreground="White"
                                        Command="{Binding DataContext.ApproveCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                        CommandParameter="{Binding}"/>

                                <Button Content="Reject" Margin="5" Padding="5,2" Background="#DC3545" Foreground="White"
                                        Command="{Binding DataContext.RejectCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>