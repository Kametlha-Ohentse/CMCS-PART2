<UserControl x:Class="CMCS_Prototype.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="#F0F0F0">
        <Border BorderBrush="#007ACC" BorderThickness="1" CornerRadius="10" 
                Padding="30" Background="White" Width="450" Height="400" 
                HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Welcome to CMCS" FontSize="28" FontWeight="Bold" 
                           Foreground="#007ACC" HorizontalAlignment="Center" Margin="0,0,0,25"/>
                <TextBlock Text="Please Enter Details and Select Role" FontSize="16" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                <StackPanel Orientation="Horizontal" Margin="0,8">
                    <TextBlock Text="First Name:" Width="120" VerticalAlignment="Center"/>
                    <TextBox Width="250" Text="{Binding LoginInput.FirstName, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,8">
                    <TextBlock Text="Surname:" Width="120" VerticalAlignment="Center"/>
                    <TextBox Width="250" Text="{Binding LoginInput.LastName, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,8">
                    <TextBlock Text="Unique ID:" Width="120" VerticalAlignment="Center"/>
                    <TextBox Width="250" Text="{Binding LoginInput.UniqueId, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <Separator Margin="0,15"/>

                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Content="Lecturer Login (ID 5 Digits)" 
                            Padding="10" FontWeight="Bold" Foreground="White"
                            Background="#007ACC"
                            Command="{Binding LecturerLoginCommand}"/>

                    <Button Grid.Column="2" Content="Admin Login (ID > 5 Digits)" 
                            Padding="10" FontWeight="Bold" Foreground="White"
                            Background="#DC3545"
                            Command="{Binding AdminLoginCommand}"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>